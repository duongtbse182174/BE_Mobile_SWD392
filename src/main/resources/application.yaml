server:
  port: 8080
  servlet:
    context-path:

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/InventoryManagement"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true