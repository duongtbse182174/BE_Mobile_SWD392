server:
  port: 8080
  servlet:
    context-path:

spring:
  datasource:
    url: jdbc:mysql://3306.mysql.database.azure.com:3306/InventoryManagement?useSSL=true&serverTimezone=UTC
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
